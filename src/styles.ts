import { createGlobalStyle } from 'styled-components' // é atraves disso que a gente tem acesso do DOM do html

const EstiloGlobal = createGlobalStyle`
  *{
margin:0;
padding:0;
}
`

export default EstiloGlobal
